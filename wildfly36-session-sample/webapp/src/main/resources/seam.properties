# Required marker: Seam scans archives that contain seam.properties (even when empty).
# See seam-reference-guide Configuration.xml — without this file, @Name components in the WAR may not install.
org.jboss.seam.core.init.debug=true
